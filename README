0. You need to have prior revision before placing these script as part of git-hook scripts!
1. use preparedb.py to sync your current schema to versioning db (internals of gitsql). eg: ./preparedb.py -d dbname <SHA1 of current revision>
2. place the pre-commit and post-pull hook
3. start committing
